const USER_ARGV_INDEX = 2;
const ExitCode = {
  error: 1,
  success: 0
};
const SSR_PORT = 8080;
const STATIC_DIR = `src/express/static`;

export = {
  USER_ARGV_INDEX,
  ExitCode,
  SSR_PORT,
  STATIC_DIR
};
